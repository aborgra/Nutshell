import React from "react"
import { Route } from "react-router-dom"

export default () => {
  return (
    <>
    {/*
    <ProviderProvider>
      <Route exact path="/" render={
        props => {
          return (
            <MessageList {...props} />
            <FriendsList {...props} />
            <EventsList {...props} />
            <NewsList {...props} />
            <TaskList {...props} />
          )
        }
    </ProviderProvider>
    */}
    </>
    
  )
}